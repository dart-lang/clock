name: clock
version: 1.0.2-dev
description: A fakeable wrapper for dart:core clock APIs
homepage: https://github.com/dart-lang/clock

environment:
  sdk: '>=2.1.1 <3.0.0'

dependencies:
  meta: ^1.0.0

dev_dependencies:
  test: ^1.0.0
  pedantic: ^1.8.0
